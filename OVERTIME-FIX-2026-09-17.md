# 2026-09-17 加班休息計算修正

依使用者確認：先累計四小時工作，接下來三十分鐘才是休息；不得從前面已工作的四小時回扣。休息結束後重新累計下一段四小時。

## 範例

| 日期類型與時段 | 新計算 |
| --- | --- |
| 平日 04:30–08:30 | 4 小時 |
| 平日 04:30–09:00 | 4 小時（08:30 後為正常工時） |
| 平日 04:00–08:30 | 4 小時（08:00–08:30 休息） |
| 平日 06:00–09:00 | 2.5 小時 |
| 假日 08:00–12:15 | 4 小時（12:00–12:15 落在休息內） |
| 假日 08:00–13:00 | 4.5 小時 |
| 假日 08:00–16:30 | 8 小時（12:00–12:30 休息；16:30 才開始下一次休息） |

修改前後端共用的 `functions/src/overtime.ts`，沒有修改 UI、打卡、帳號、資料庫規則、平日正常工時排除或原有半小時取整方式。已傳入計算器的區間按時間順序處理，位於休息期間的空檔不重扣，也不將前面的空檔提前抵扣後面的休息。既有一小時以上間隔重新起算的政策保留。

本次不擴大處理相鄰申請單的收集／分配、取消重疊單後重算等既有暫緩事項；也不批次修改歷史紀錄。已儲存的舊版 3.5 小時不會自動變成 4 小時，須另行核對處理。

## 驗證

- 修正前新增測試成功重現四小時被算成 3.5 的錯誤。
- 修正後 23 項邏輯測試通過（Node 24 與 Node 20）。
- 28 個本機 Firebase 模擬器整合情境通過（test runner 顯示 29，含外層測試）。包含提交後實際儲存 4 小時、核准不重扣、修改時重算及舊權威時數保留。
- TypeScript 檢查、前端正式建置、Functions Node 20 編譯通過。Vite 仍有既有的大型 bundle 警告。
- 沒有登入正式員工帳號、修改正式資料或自動部署；正式手机／網路操作仍需上線後驗收。

## 更新方式

1. 使用新版 `cloud-function-deploy-20260917.zip`，不可再用 20260915 的舊部署包。
2. 解壓縮後在含 firebase.json 的根目錄安裝／編譯：

```bash
npm --prefix functions ci
npm --prefix functions run build
```

3. 本次只需部署受影響的 Function：

```bash
firebase deploy --only functions:secureAttendance --project shyuan-hrm
```

4. 再將 `github-upload-ready` 內新版檔案更新至 GitHub，等 Vercel 顯示 Ready，再重新整理加班頁面。
5. **本次不用更新 Firestore／Storage Rules，也不用重建帳號或集合。** 不需重新部署密碼重設兩支 Functions。

在前後端更新過渡期，舊分頁可能仍顯示舊預估時數，因此應接續完成兩端更新並重新整理。先以日期／時間選擇確認預覽，不必為測試在正式帳號送出不實申請。
