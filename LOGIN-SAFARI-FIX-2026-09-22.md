# 2026-09-22 登入恢復與 Safari 連線顯示修正

## 範圍

僅修改前端；不用重新部署 Cloud Functions、Firestore Rules 或 Storage Rules。
將 github-upload-ready 內的檔案更新至既有 GitHub 儲存庫根目錄，等待 Vercel 正式部署 Ready。
本次未替使用者推送 GitHub、部署雲端或修改正式資料。

## 原因與修正

- 原本 Firebase 尚未恢復登入，就先顯示登入表單；現在驗證期間顯示簡短的恢復登入畫面，確認有效後直接進入原有介面。
- 帳密登入與 App 的 Auth listener 原本各查一次員工資料、各啟動一次同步。改為單一 listener 負責，避免重複及競態。
- 改用 initializeAuth，移除帳密登入不需要的 popup/redirect resolver，避免手機瀏覽器預先載入驗證 iframe。保留既有 localStorage、IndexedDB、sessionStorage 登入狀態的遷移路徑；不保存密碼。
- 暫時網路錯誤／讀取逾時不再直接登出；顯示重新連線。員工資料等候上限 12 秒，逾時後的舊結果不得恢復已登出的帳號。停用、缺少、重複、UID 不符等帳號仍禁止進入。
- 公司設定、自己的資料或打卡紀錄尚未同步時，打卡按鈕不可操作；不再把未載入誤寫成「不限距離」或「尚無紀錄」。成功讀取空紀錄後才顯示尚無紀錄。
- 缺少本機快取不再誤判為帳號被刪除；伺服器確認的刪除或權限拒絕仍會結束登入。
- 避免可選的帳號提示儲存失敗導致整個登入流程失敗。
- 校時使用不依賴 Promise.any 的同等實作，保留多來源第一個成功回應及失敗禁止打卡的行為。
- 時鐘區塊禁止 flex 壓縮；未更改主介面操作流程、加班／請假算法、後端位置與時間驗證。

## 診斷限制

截圖的 auth/network-request-failed 表示 Firebase 登入網路請求失敗，並非密碼錯誤證據。
使用者已確認 Wi-Fi、行動網路都相同；仍待 iOS 版本與該手機實際測試，無法僅憑截圖認定根因或保證修復。
Firebase 初始化調整有官方依據：https://firebase.google.com/docs/auth/web/custom-dependencies
自動登入依賴同一正式網址、瀏覽器允許儲存、未主動登出及有效登入狀態。不能保證所有網路下零等待。

## 驗證

- npm test：36 項通過（23 項原回歸、13 項登入／同步／相容性測試）。
- npm run typecheck：通過。
- npm run build：通過；仍有既有大型 bundle 提醒。
- 新測試使用模擬 Firebase 事件，不連正式資料；尚未在該舊 iPhone 實測。

## 上線後測試

1. 一般 Safari、同一正式網址登入一次，關閉分頁再開：不先出現帳密表單，恢復有效登入後到打卡頁。
2. 主動登出後再開網址：必須顯示登入表單。
3. 斷網／弱網：顯示讀取或重新連線提示，不把缺資料顯示為不限距離，不可離線打卡。
4. 重新連線後，確認時間、公司位置及原有紀錄恢復；依正常上下班時機實際打卡，避免產生測試考勤。
5. 確認管理員、員工兩種帳號、強制改密碼帳號及請假加班操作沒有回歸。
6. 提供出問題手機的 iOS 版本；如果更新後仍失敗，請提供已遮蔽密碼的新錯誤截圖。
