# 安全修正版驗證紀錄

本次未部署、未讀寫正式 Firebase 業務資料、未測試任何真實密碼。測試帳號均為本機模擬器合成資料。

## 範圍

- 員工頁面布局／按鈕保持，資料寫入轉到可信後端。
- 前後端型別檢查與正式建置。
- Node 20.20.2（與 Functions 指定 major 相同）及本機 Node 24 的回歸測試。
- Firebase CLI 15.30.1、Rules 測試工具 5.0.2、Firestore Emulator 1.22.0、Storage Rules Runtime 1.1.3、Java 21。
- 模擬專案固定 `demo-hrm-secure-test`，Auth／Firestore／Storage 只綁定 127.0.0.1；測試程式會拒絕非本機主機設定。

## 測試項目

結果：18 項純邏輯測試及 26 個模擬器整合情境均通過（Node 20 與 Node 24）。整合測試直接執行 callable 處理函式及本機安全規則，不代表已驗證正式 HTTP 呼叫、部署權限或手機操作。

最後檢查：主資料夾及 GitHub 上傳資料夾型別檢查／Vite 建置均成功，兩份產物均為 `index-Da_m9Lbx.js`；Functions 亦已使用 Node 20 編譯並通過上述整合測試。

`tests/regression.cjs`：18 項純邏輯測試，包括無效日期、台灣時區、12:00–13:00 午休、跨日請假、額度不足、取消退款、分批額度、不完整舊扣抵、附件驗證、定位設定失效、CSV 公式、兩張卡均缺、部分請假與遲到、OUT 在 IN 之前、秒數邊界、伺服器時間與管理員補卡。

`tests/emulator.cjs`：26 個整合情境（Node test runner 顯示 27，含外層測試），包括：

- 本人資料讀取、跨人員與未登入拒絕。
- 直接偽造打卡、直接核准、清除強制改密碼標記遭拒。
- 封存／無員工資料／待改密碼帳號不能上傳。
- 新舊附件目錄相容、UID 不同於帳號 ID、禁止覆寫、跨員工讀取拒絕。
- 伺服器忽略偽造打卡時間與距離、同時打卡只成功一次、重試不重複。
- 下班範圍外仍記錄異常、上班範圍外拒絕；管理員補卡保留選定時間。
- 後端重算請假／加班時數，拒絕 malformed 附件。
- 兩張假單同時核准正確扣額度、重複核准不重扣、不足維持待審。
- 相同舊數字 ID 不會誤核准另一張單。
- 員工取消本人待審請假／加班成功，不能取消他人或已核准的申請。
- 管理員取消已核准假單只退款一次。
- 永久刪除員工與改寫稽核日誌遭拒。
- 封存停用 Auth、恢復重新啟用。
- 舊文件偽造 UID／附件路徑無法诱使管理員刪除其他員工的檔案。
- 新密碼相同／舊密碼錯誤不解除限制，Auth 密碼成功更新才解除。

模擬器輸出的部分 PERMISSION_DENIED 是上述拒絕案例的預期結果，不是所有操作失敗。測試未使用生產網站或生產資料。

## 限制與尚需人工驗收

- 沒有執行正式手機 Safari／Chrome 的完整畫面操作驗收，沒有登入正式員工帳號。
- 沒有驗證正式部署的 IAM、App Check、實際網路／定位精度或正式資料中每一筆舊額度。
- Vite 建置仍有約 977 KB 主 bundle 過大的既有警告；不影響建置完成，但效能重構未在本批進行。
- 沒有自動修改歷史額度或重新計算所有歷史加班；尚未修正項目請見安全更新說明。

## 交付整理

主資料夾與 `github-upload-ready` 同步來源程式、規則、Functions 原始碼及測試／部署文件。上傳包不含 node_modules、測試下載工具、舊 dist 或歷史對話。

原上傳包的舊 dist 移至 `.verification/previous-upload-dist`，可恢復；主資料夾 dist 為本次重新建置產物。後端部署壓縮包另附預先編譯的 functions/lib；新版來源也保留 build 前置檢查。備份與模擬器資料均不需上傳。
